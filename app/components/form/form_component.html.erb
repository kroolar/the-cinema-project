<form
  action="<%= action %>"
  method="<%= method %>"
>
  <input
    type="hidden"
    name="authenticity_token"
    value="<%= form_authenticity_token %>"
    autocomplete="off"
  >
  <%= content %>
</form>